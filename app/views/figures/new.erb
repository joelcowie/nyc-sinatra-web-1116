<!DOCTYPE html>
<html>
	<form action="/figures" method="post">
		<p> figure_name: <input id="figure_name" type="text" name="figure[name]"></p>
		<p> Landmark name: <input id="new_landmark" type="text" name="landmark[name]"></p>
		<p> Title name: <input id="new_title" type="text" name="title[name]"></p>

		<%Title.all.each do |title| %>
			<label for""> <%=title.name%> </label>
			<input id="title_<%="#{title.id}"%>" type="checkbox" name="figure[title_ids][]" value="<%=title.id%>"></input>
		<%end%>	

		<%Landmark.all.each do |landmark| %>
			<label for""> <%=landmark.name%> </label>
			<input id="landmark_<%=landmark.id%>" type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>"></input>
		<%end%>

		<button type="submit"> Create New Figure  </button>
	</form>	
</html>
